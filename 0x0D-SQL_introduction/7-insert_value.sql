#!/usr/bin/env bash
-- Script to insert a new row into the table first_table in the specified database
-- Insert a new row into the first_table

INSERT INTO first_table (id, name) VALUES (89, 'Best School');
